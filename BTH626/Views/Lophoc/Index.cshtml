@model IEnumerable<BTH626.Models.Lophoc>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr> <th>
                @Html.DisplayNameFor(model => model.tenlop)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.malop)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tenlop)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.malop)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.tenlop">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.tenlop">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.tenlop">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
